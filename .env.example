# Build assist (OpenAI-compatible chat API)
OPENAI_API_KEY=
# Optional overrides
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_API_BASE=https://api.openai.com/v1

# Site password gate (leave empty to disable locally; required for production)
APP_PASSWORD=
